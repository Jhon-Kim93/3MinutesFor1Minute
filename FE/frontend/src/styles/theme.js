import _ from './_variables';

const theme = {
	base: {
		backgroundColor: _.darkWhite,
		sidebarColor: _.deepGrey,
		footerColor: _.lightGrey,
		fontColor: _.black,
		subFontColor: _.darkWhite,
		accentColor: _.oceanBlue,
		warnColor: _.darkRed,
		confirmColor: _.green,
		timerColor: _.blueGreen
	},
};

export default theme;
